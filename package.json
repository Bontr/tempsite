{
  "name": "bontrwebsite",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "npm run check:scene && astro build",
    "build:pages": "npm run check:scene && cross-env DEPLOY_TARGET=github-pages astro build",
    "preview": "astro preview",
    "check": "astro check",
    "check:scene": "node scripts/validate-scene-assets.mjs",
    "generate:scene": "node scripts/build-scene-data.mjs"
  },
  "dependencies": {
    "astro": "^7.3.2",
    "gsap": "^3.15.0",
    "three": "^0.186.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@types/node": "^22.20.2",
    "@types/three": "^0.185.4",
    "cross-env": "^10.1.0",
    "typescript": "^5.9.2"
  }
}
